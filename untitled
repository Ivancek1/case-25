from main.main import decrypt_text_caesar


def break_text_ceasar(text: str) -> str:
    res = []
    
    for i in range(25)
        res.append(decrypt_text_caesar(text, i))
    
    return '\n'.join(res)
    
print(break_text_ceasar('JGuux kbx'))